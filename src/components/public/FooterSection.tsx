import React from 'react';
import { Restaurant } from '@/types/supabase';
import { Button } from '@/components/ui/button';
import { Phone, Mail, Link, ExternalLink } from 'lucide-react';

// ... (restante do arquivo)