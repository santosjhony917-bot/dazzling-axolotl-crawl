import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.dazzlingaxolotlcrawl',
  appName: 'dazzling-axolotl-crawl',
  webDir: 'dist'
};

export default config;
